apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
data:
  prometheus.yml: |
    global:
      scrape_interval: 5s

    scrape_configs:
      - job_name: "failure-playground-api"
        metrics_path: "/prometheus"
        static_configs:
          - targets: ["api-service:8000"]
